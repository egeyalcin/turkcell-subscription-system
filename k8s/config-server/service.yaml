apiVersion: v1
kind: Service
metadata:
  name: config-server-svc # Diğer servisler bu isimle bağlanacak
  namespace: turkcell
spec:
  selector:
    app: config-server
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888
  type: ClusterIP