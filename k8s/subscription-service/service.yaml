apiVersion: v1
kind: Service
metadata:
  name: subscription-service
  namespace: turkcell
spec:
  type: NodePort
  selector:
    app: subscription-service
  ports:
    - port: 8017
      targetPort: 8017
      nodePort: 30001